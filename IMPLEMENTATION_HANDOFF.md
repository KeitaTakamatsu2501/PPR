# PPR実装の引き継ぎ指示

この文書は、別のAIへそのまま渡す実装依頼である。計画作成者との会話を知らなくても着手できるよう、承認済みの範囲と実行方法を固定している。

## 依頼

`/Users/keita/dev/private/PPR` に、JTSの既存デスクトップキャラクタービルダーから独立したPPRを実装してください。

以下の三文書を順に読み、実行計画のE0〜E9を完了してください。

- `docs/EXECUTION_PLAN.md`
- `docs/IMPLEMENTATION_PLAN.md`
- `docs/ACCEPTANCE.md`

計画書の読了や雛形作成で終了せず、独立起動・編集・ワークショップ・JTS入出力・比較試演・最小SBT試演が機械的受入条件を満たすところまで実装します。

## ユーザーが回答済みの事項

- 初期UIは既存のTkデスクトップUIと操作を継承する。Web化しない。
- 今回は独立ビルダー、JTS入出力、試演、最小のSBT試演まで作る。
- PPRを人格の編集元とし、JTSには選んだ版を明示操作で反映する。
- LLM接続は現在のJTSの接続方式を継承する。
- 人物らしさは作者の感覚で決める。同一性の自動採点・自動矯正は作らない。
- SBTは企画段階。SBT本体の実装を探したり、ゲーム全体を新設したりする必要はない。
- 価値観を詳細に記述する既存方式を維持する。物語・来歴の作成や性格スコアへの圧縮を要求しない。

上記の再確認は不要。通常の関数分割、UI配置、例外名などは計画と整合する範囲で実装者が決める。

## 作業上の境界

- 実装先はPPR。参照元は `/Users/keita/dev/private/JTS` と `/Users/keita/dev/private/SBT`。
- JTSには調査時点で多数の未コミット変更と未追跡ファイルがある。**現在の作業ツリー**を読む。HEADへの復元、stash、clean、既存変更の取り消しはしない。
- JTS/SBT/MyFriendsのソースを編集しない。PPRの通常起動でこれらをimportしたり、`sys.path`へ追加したりしない。
- JTS人格データの読取とPPRへのコピーは実装・検証の対象。本番JTSへの書き込みは、完成したアプリの「JTSへ反映」操作で作者が選ぶ。開発用テストはJTS配置の複製に対して行う。
- 音声モデル・大量の生成ログ・認証ファイルをPPRへ一括コピーしない。接続設定は許可リストの項目だけを取り込む。
- 機械的受入が通ったら実装は完了報告できる。作者の主観評価をAIが代行せず、未実施ならそのまま記載する。主観評価待ちで無関係の実装を止めない。

## 最初に行うこと

1. この作業環境に適用される指示を確認する。
2. `docs/SOURCE_BASELINE.json` と現在の参照元を照合する。差分があれば関係箇所を読み直し、現在の作業ツリーから抽出元スナップショットを作る。ハッシュ差だけでユーザーへ質問しない。
3. Python 3.14 / Tk 9の環境を確認し、PPR専用venvを作る。
4. E0から実装する。データ保持と独立起動を先に成立させる。

## 再開方法

進捗は `docs/IMPLEMENTATION_STATUS.md` にE0〜E9の状態、実装済みファイル、実行したコマンド、残作業を記録する。このファイルは実装開始時に作成する。再開時は完了済み作業を繰り返さず、対応する受入項目の結果を確認して次へ進む。

並列化する場合も、`domain.py`、保存形式、provider interface、scheme interfaceを先に固定する。GUI、JTS adapter、LLM transport、試演adapterの担当を分け、同じファイルの同時編集を避ける。

## 完了報告

次を簡潔に示す。

- 起動方法と使える機能。
- 機械的受入項目の合否。実行していない検証を合格と書かない。
- 実データの取込・未編集書き出しの一致結果。
- 元JTSへ変更を加えていないこと。
- 実モデル通信・実GUI確認・作者による人物らしさの確認の実施状況。
- 既知の制限。特にPPRのJTS会話試演は現行JTSの全生成工程を再現したものではないこと。
